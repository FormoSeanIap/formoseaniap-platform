# Repo-local Codex default: no approval prompts for in-repo work.
# Keep access limited to this workspace and keep network disabled.
approval_policy = "never"
sandbox_mode = "workspace-write"
allow_login_shell = false

[sandbox_workspace_write]
network_access = false
